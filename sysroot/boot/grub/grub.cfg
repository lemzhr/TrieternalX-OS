# File: sysroot/boot/grub/grub.cfg

set timeout=3
set default=0

menuentry "ProyekOS Skripsi" {
    # 'kernel.bin' adalah nama file kernel kita setelah di-link
    # GRUB akan memuatnya di alamat 1MB (sesuai linker.ld)
    multiboot /boot/kernel.bin
    boot
}